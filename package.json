{
  "name": "pace-converter",
  "private": true,
  "version": "1.0.0",
  "description": "Running pace ruler — min/km and min/mi with finish times from a 5K to a 300-mile ultra",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fontsource/archivo": "^5.2.5",
    "@fontsource/archivo-black": "^5.2.5"
  },
  "devDependencies": {
    "happy-dom": "^18.0.1",
    "typescript": "^5.9.2",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
